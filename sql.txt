apt-get install mariadb-server
mysql -u root -p
exit
show databases;
create database if not exists my_db;
use mydb;
show tables;
CREATE TABLE persons (
    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    age int NOT NULL,
    birth_date DATE,
    phone VARCHAR(15) NOT NULL UNIQUE,
    country VARCHAR(30) NOT NULL DEFAULT 'Australia',
    salary INT NOT NULL CHECK (salary >= 3000 AND salary <= 10000),
);
The CHECK constraint is used to restrict the values that can be placed in a column
CREATE TABLE society(
FOREIGN KEY (PersonID) REFERENCES Persons(id);
);

DESCRIBE persons;
INSERT INTO persons (name, birth_date, phone,age)
VALUES ('Peter Wilson', '1990-07-15', '0711-020361',29);
SELECT * FROM my_table;
SELECT id, name, age, phone FROM my_table;
SELECT * FROM person WHERE age >= 28 and/or name='shubham'; {WHERE name LIKE 'Dav' and (WHERE name IN ('shubham', 'rohan') or WHERE age BETWEEN 23 AND 30}
SELECT * FROM person ORDER BY name ASC/dsc;
SELECT TOP 3/limit 3 * FROM employees
ORDER BY salary DESC;
SELECT DISTINCT country FROM person;
UPDATE person SET name = 'Sarah' WHERE age = 28;
DELETE FROM persons WHERE age > 28;
DELETE FROM persons;/DROP TABLE persons;/DROP DATABASE demo;
RENAME old_table _name To new_table_name ; 
